name: das-v2
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pytorch
  - cpuonly
  - pip:
      - numpy
      - pandas
      - scipy
      - matplotlib
      - seaborn
      - scikit-learn
      - scikit-image
      - pillow
      - tifffile
      - tqdm
      - patsy
      - umap-learn
      - openpyxl
      - xlrd
      - shapely
      - pygame
      - czifile
      - tensorflow-cpu
      - csbdeep
      - stardist
